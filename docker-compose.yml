version: '3.8'

services:
  devops-green-agent:
    build: .
    container_name: devops-green-agent
    ports:
      - "9009:9009"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/workspace:ro
    restart: unless-stopped
